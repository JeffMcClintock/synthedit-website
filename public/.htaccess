# This file is deployed into the site's folder on the server (public_html/_site/).
# Routing for the whole domain is handled by public_html/.htaccess — see
# server/root.htaccess in this repo for the copy under version control.
<IfModule mod_dir.c>
    DirectoryIndex index.html
</IfModule>
